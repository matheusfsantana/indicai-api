/*const mongoose = require( 'mongoose' )

const indicacaoSchema = require('./Indicacao')

const {userSchema} = new mongoose.Schema( {
  name: {
    type: String,
    required: true
  },
  email: {
    type: String,
    required: true
  },
  indicacoes:{
    type: {indicacaoSchema},
    required: false
  }


}, { collection: 'user' } );

const User = mongoose.model("User", userSchema);

module.exports = User;*/



